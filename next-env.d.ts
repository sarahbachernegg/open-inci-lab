# Open INCI Lab does not require environment variables for the local demo.
# Keep real values in .env.local and never commit them.

# Optional placeholders for a future backend integration:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
